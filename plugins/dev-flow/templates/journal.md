---
doc: F-00x-<slug>
branch: build/F-00x-<slug>
base: <開分支時主線的 sha>
updated: <YYYY-MM-DD>
---
# 開發日誌:F-00x-<slug>

## 做了什麼
- 骨架:<n> 條 step,測試檔 `<路徑>`
- 簽名 <m> / <n> · 觀察點 <j> / <k> · laws <g> / <k> · examples <g> / <k>
- <達成,status 改 frozen | 停在 GAP-n、GAP-m>

## 動到的檔
- 自己的:`.design/features/F-00x-<slug>.md`、`<程式碼檔>`、`<測試檔>`
- 共用的:`<建置設定>`(<改了哪幾行:登記了哪些檔案>)、`<共用檔案>`(<新增了哪些名字>);無則「無」

## 決定
- qa:<產生器的分佈與尺寸、案例數上限,一條一句>
- impl:<資料結構、演算法、私有 helper,一條一句>

## GAP
- <GAP-n(F-00x#LAW-n / qa):需要回答什麼>;無則「無」

## 測試
- 指令:`<整套指令>`,log:`<路徑>`
- 綠 <g> 條 · 紅 <r> 條(<紅的各對到哪條 law、歸因>);無紅寫「全綠」

## 合併時要看
- 與 <F-00y-<slug>> 的 step 都住 `<檔案>`;無則「無」
- 別份也可能動的檔:<建置設定>;無則「無」
- 合併後預期:<什麼會變;無則「無」>
