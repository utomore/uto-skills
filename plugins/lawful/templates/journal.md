---
key: M-n-<slug>
branch: build/M-n-<slug>
base: <開分支時主線的 sha>
verdict: <feasible | infeasible>
updated: <YYYY-MM-DD>
---
# 決策紀錄：M-n-<slug>

## Goal / Scope
- 需求與里程碑：<R-n-<slug>> · M-n-<slug>:<里程碑那一句>
- 做到：<這一片從哪個入口貫通到哪個出口，一條一句>
- 明確沒做：<屬於這條里程碑、這一片卻沒碰的；無則「無」>

## Entry
- `<一道指令:怎麼把它跑起來看到行為>`

## Decisions
| # | Decision | Reason | Constraint | 否決 | 可逆 | 跨文檔 |
|---|---|---|---|---|---|---|
| D-1 | <為了達成這條里程碑，決定了什麼> | <為什麼，一句> | <這個決定受什麼約束：全域 Law 的哪一條（INV-n、四層的規則、對外 I/O 的契約）、需求的驗收、外部系統；沒有寫 -> | <沒選的做法> | <是 | 否> | <是 | 否> |

## Assumptions & Invariants
| 假設或不變量 | 來源 | 在哪 |
|---|---|---|
| <這一片當成成立的前提，一句可判定的話> | <刻意 | 順手> | `<模組>` 的 `<名字>` |

## Faked / Unverified
| 什麼 | 在哪 | 真的該是什麼 |
|---|---|---|
| <假資料、寫死的值、沒接上的真解譯器、沒驗過的路徑> | `<模組>` | <一句> |

## Touched
- 模組：<動到或新增的模組，各標層與模組單元>
- 共用型別：<新增或改了建構子、欄位的 types 層型別；無則「無」>
- 對外 I/O:<新的入口與出口；無則「無」>
- 建置設定：<改了哪幾行；無則「無」>

## Verification
- 文檔：<全名，簽名 m / n · 觀察點 j / k · laws g / k · examples g / k；達成，status 改 verified | 停在 GAP-n>
- 首跑該紅：<開發者答「不對，應該是 …」的 law，寫 P-00x#LAW-n，「、」分隔；無則「無」>
- 首跑：<該紅的幾條裡首跑紅了幾條、該綠的幾條裡首跑綠了幾條；該紅卻綠退回 qa 幾條>
- qa 的決定：<產生器的分佈與尺寸、案例數上限，一條一句>
- refactor 的決定：<資料結構、演算法、私有 helper，一條一句；整份重寫的註明>
- GAP:<GAP-n(P-00x#LAW-n / qa)：需要回答什麼>；無則「無」
- 測試：`<整套指令>`，log `<路徑>`，綠 <g> 條 · 紅 <r> 條（<紅的各對到哪條 law、歸因>）；無紅寫「全綠」

## 合併時要看
- 與 <P-00y-<slug>> 的 stage 都住 `<模組>`；無則「無」
- 別條也可能動的檔：<建置設定>；無則「無」
- 合併後預期：<什麼會變；無則「無」>
