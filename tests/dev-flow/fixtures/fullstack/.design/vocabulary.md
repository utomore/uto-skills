# 名詞
名詞只住這個檔：新增或調整任何一個領域名詞的定義，一律改這張表，不寫在別處。需求的一句話、驗收、里程碑那一句與 law 的第一句用到的領域名詞，都在這張表上。

| 名詞 | 定義 | 型別 |
|---|---|---|
| 金額 | 以分為單位、單一幣別內的一筆錢；不是帶小數的數字 | `Money` |
| 訂單 | 一筆結帳成功留下的紀錄，帶著付了多少；結帳失敗不產生訂單 | `Order` |
| 退款 | 對一筆訂單退回的錢的紀錄；不是取消訂單 | `Refund` |
| 購物籃 | 一位顧客還沒結帳的品項清單；不是訂單 | `Basket` |
