$ devflow brief require-design --no-rules
brief require-design (沒有目標) @doc:- rules:<雜湊>
這一行是指紋:被委派的角色在回報的第一項照抄它。以下是開工要的全部,不必再去找規章、文檔或宣告。

# 規章

(--no-rules:規章的節這一場已經給過,不重印)

# system.md(全份)

````markdown
---
language: <typescript | javascript | python | go | rust;前後端各一種語言就寫 [web = typescript, api = python]>
updated: <YYYY-MM-DD>
---
# <專案名>:<一句話>

## 願景
<北極星,第一段一到三句:這個專案要交出的、世界上還沒有的東西是什麼,替誰改變了什麼。後面可以展開:替誰做什麼、明確不做什麼;不隨里程碑變。>

## 全域 Law
不得違反:整個專案任何一條切片、任何一份 feature 都要守。三類各住一區,各有一道 lint 自動確認(`devflow lint global` 一次查完)。每一條都是從做出來的切片裡抽上來的:`dev-flow:scope-laws` 對著切片談出候選,開發者明確批准,`dev-flow:global-laws` 落筆;修改、放寬、替換或刪除同樣要開發者明確批准,一律由 `dev-flow:global-laws` 寫。

### 領域不變量
無

### 架構:層
由內而外;內層不准 import 外層,最後一列是最外層,也是唯一能做對外 I/O 的層。

| 層 | 裝什麼 |
|---|---|

### 契約:對外 I/O
跨過對外邊界的資料要守什麼:信任(`trusted` / `untrusted`)、驗證(`untrusted` 的入口第一個碰到資料的 step),與守這一端的 law(`F-00x#LAW-n` 或 `INV-n`,沒有寫 `-`)。

| 名稱 | 方向 | 型別 | 模組 | 進入哪份 feature | 信任 | 驗證 | 契約 |
|---|---|---|---|---|---|---|---|

## Constraint
硬性限制:寫程式之前就定得下來、每一行程式碼與測試都照做的規定,與工具要讀的那幾行(三道指令、追加清單、忽略目錄、號段、優先)。開發者定,`dev-flow:kickoff` 寫,之後隨時回 `dev-flow:kickoff` 補或改;限制的類別可以自己加。
- 語言與版本:<例如 TypeScript 5.4;無則「無」>
- 編譯器與執行環境:<例如 Node 20、tsc strict;無則「無」>
- 套件與框架:<硬性要求或禁用的套件、框架與版本;無則「無」>
- 環境:<作業系統、部署目標、容器;無則「無」>
- 命名與寫法:<變數、函數、檔案怎麼命名,格式與風格;無則「無」>
- 建置:`<指令>`
- 測試(整套):`<指令>`
- 測試(子集):`<指令,以一份文檔選>`
<多語言專案上面三道每側一段:- 測試(整套):web = `npx jest`;api = `pytest`>
- IO 模組追加:<這個專案自己的 IO 模組或客戶端,無則「無」>
- Laws 詞彙追加:<law 會用到、但不是 Steps 簽名也不是型別名的字,無則「無」>
- 忽略目錄:<不掃的原始碼目錄,無則「無」>
- 號段:<兩人以上平行 claim 時每人一段,以 git 的 user.email 為鍵:a@corp.com = 000-099;b@corp.com = 100-199;無則「無」>
- 優先:<1 = 什麼;2 = 什麼;3 = 什麼;4 = 什麼>

## Features
| 全名 | 類別 |
|---|---|
| F-001-<slug> | feature |
````

# 需求檔

(requirements/ 底下一檔都沒有)

# devflow status

### devflow status
需求 0 條,已驗收 0 條 · 等人工審核 0 條 · 領域不變量 0 條,成立 0 條 · 里程碑 0 條,達成 0 條 · feature 0 份,達成 0 份 · 文檔共 0 份,達成 0 份 · 還沒實作的 step 0 個 · 還開著的 GAP 0 條
· 此 adapter 不解析測試輸出

#### 需求
- 沒有任何需求;dev-flow:require-design 談第一條

#### 全域 Law
| 類別 | 住 system.md 的哪一區 | 自動確認 | 結果 |
|---|---|---|---|
| 架構 | 全域 Law › 架構:層 | devflow lint boundary | 0 層,沒有程式碼可對 |
| 契約 | 全域 Law › 契約:對外 I/O | devflow lint io | 0 列,通過;契約欄指到的 law 0 條,成立 0 條 |
| 領域不變量 | 全域 Law › 領域不變量 | devflow lint invariants | 0 條,通過;成立 0 條 |

- 領域不變量:無

#### 文檔
| 文檔 | 類別 | status | 文檔簽名數 | 程式碼對到 | 未實作 | law 條數 | law 綠/翻 | REV | 狀態 |
|---|---|---|---|---|---|---|---|---|---|

#### 1. 今天能開幾條線
- 無

#### 2. 卡住的
- 無

#### 3. 等決定
- 無

#### 4. 牽動誰
- 無

#### 5. 待實作(按檔案)
- 無

#### 6. 修訂熱點
- 無(還沒有任何 REV)

#### 7. 警訊
| 哪裡 | 什麼事 | 怎麼辦 |
|---|---|---|
| system.md | 願景還是模板 | dev-flow:kickoff 訂願景 |
| .design/ | 沒有任何需求 | dev-flow:require-design 談第一條需求(至少一條) |

#### 8. 建議路線
- 還沒有任何文檔;先 dev-flow:require-design 談需求、切里程碑,再 dev-flow:spike-impl <M-n-slug>
exit 0
