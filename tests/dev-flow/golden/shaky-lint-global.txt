$ devflow lint global
## lint boundary:3 條不合規
- ✗ .design/modules.md:8 模組表有 src/gone/**,程式碼裡沒有檔案對得到(幽靈)
- ✗ src/domain/rule.ts domain 層 import 了更外面的 application 層:src/app/store.ts
- ✗ src/helper.ts 不在模組表(未登記),它在哪一層沒有人知道
## lint io:6 條不合規
- ✗ .design/system.md:31 POST /score 是 untrusted 的入口,驗證欄要指名做驗證的 step
- ✗ .design/system.md:31 POST /score 的契約 F-001#LAW-9 不存在;契約欄寫守這一端的 law
- ✗ .design/system.md:31 POST /score 的契約 INV-9 不在 system.md 的領域不變量裡
- ✗ .design/system.md:31 POST /score 的契約「冪等」要是 F-00x#LAW-n 或 INV-n;沒有就寫 -
- ✗ .design/system.md feature F-002-other 沒有任何對外 I/O 列;feature 的兩端都要對得到這張表
- ✗ .design/features/F-001-score.md:34 F-001-score 的 EX-1 字面值出現「api_key」;秘密不進文檔,example 用假名或型別描述
- · src/entry/debug.ts 在最外層而且 import 了 node:fs,但不在對外 I/O 表;可能是沒登記的出入口
## lint invariants:4 條不合規
- ✗ .design/system.md:15 INV-2 |- 行的 score 不是最內層的匯出、型別名或標準函式庫;領域不變量只引用最內層共用的東西,提到某一份 feature 的簽名它就是那份 feature 的 law
- ✗ .design/system.md:15 INV-2 編號重複;配號只走 devflow invariant add
- ✗ .design/system.md:19 INV-3 種類「nonsense」不在 invariant / identity / roundtrip / relation / bound / equiv / total / commute;第一行寫成 INV-3 [種類] 一句話
- ✗ INV-2#LAW 寫了三行卻沒有測試;領域不變量只由測試判,沒有測試就是未知
- · INV-1#LAW 還沒有三行式也沒有測試,成立與否未知;領域不變量只由測試判
- · INV-3#LAW 還沒有三行式也沒有測試,成立與否未知;領域不變量只由測試判
exit 1
