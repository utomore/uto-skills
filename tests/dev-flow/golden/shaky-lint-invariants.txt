$ devflow lint invariants
## lint invariants:7 條不合規
- ✗ .design/system.md:14 INV-1 沒有三行;領域不變量是從一條 law 抽上去的,把出處那條 law 的三行照搬過來(識別字只用最內層的匯出與型別名),沒有出處的不立
- ✗ .design/system.md:15 INV-2 |- 行的 score 不是最內層的匯出、型別名或標準函式庫;領域不變量只引用最內層共用的東西,提到某一份 feature 的簽名它就是那份 feature 的 law
- ✗ .design/system.md:15 INV-2 編號重複;配號只走 devflow invariant add
- ✗ .design/system.md:15 INV-2 沒有三行;領域不變量是從一條 law 抽上去的,把出處那條 law 的三行照搬過來(識別字只用最內層的匯出與型別名),沒有出處的不立
- ✗ .design/system.md:19 INV-3 種類「nonsense」不在 invariant / identity / roundtrip / relation / bound / equiv / total / commute;第一行寫成 INV-3 [種類] 一句話
- ✗ .design/system.md:19 INV-3 沒有三行;領域不變量是從一條 law 抽上去的,把出處那條 law 的三行照搬過來(識別字只用最內層的匯出與型別名),沒有出處的不立
- ✗ INV-2#LAW 沒有測試;領域不變量只由測試判,沒有測試就是未知
exit 1
