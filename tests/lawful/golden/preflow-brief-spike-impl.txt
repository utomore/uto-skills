$ lawful brief spike-impl M-2 --tests test.log --no-rules
brief spike-impl M-2 @doc:b9b36d16 rules:<雜湊>
這一行是指紋:被委派的角色在回報的第一項照抄它。以下是開工要的全部,不必再去找規章、文檔或宣告。

# 規章

(--no-rules:規章的節這一場已經給過,不重印)

# 分支與工作樹

(這個目錄不是 git repo 的根,也不是一棵工作樹)

# .lawful/ 現在有什麼

- adr/
  - ADR-001-save-format.md
- Cone.md
- modules.md
- objectives/
  - R-1-O-1-save-roundtrip.md
  - R-1-O-2-save-list.md
  - R-9-O-3-save-cloud.md
- pipelines/
  - P-001-save-write.md
- spikes/
  - SPK-001-cbor-size.md

# 需求:R-1(.lawful/Cone.md)

R-1:玩家存檔後能讀回同一個世界
- 驗收:任一 World 存檔再讀回,可存檔的投影一模一樣
  - forall w in World
  - |- decode (saveBytes w) == Right (toSave w)
- 里程碑 M-1:存檔寫得出檔案(綁定 P-001-save-write)
- 里程碑 M-2:存檔壞了看得出來(綁定 -)
- 里程碑 M-3-save-list:存檔列得出來(綁定 -)
- 里程碑 RF-1:存檔檔案壓縮後不超過 1 MB(綁定 P-001-save-write)

# Cone.md(全份)

````markdown
---
language: haskell
updated: 2026-09-05
---
# save-game:把遊戲世界存成檔案再讀回來

## 願景
玩家在任何時刻存檔,之後讀回來的世界和存檔那一刻一模一樣。

替一個小型 2D 遊戲提供存檔與讀檔。玩家按存檔,當前世界寫成一個檔案;讀檔把檔案還原成同一個世界。不處理雲端同步,不處理跨版本升級。

## 需求
### R-1:玩家存檔後能讀回同一個世界
- Law:任一 World 存檔再讀回,可存檔的投影一模一樣
  - forall w in World
  - |- decode (saveBytes w) == Right (toSave w)
- 蘊含:O-1 與 O-2 都達成,存得進去也找得回來

### R-2:存檔壞了讀得出是哪裡壞
- Law:任一壞掉的存檔,讀檔回報第一個壞掉的位置

## 專案約束
- 語言:haskell
- 建置:`cabal build`
- 測試(整套):`cabal test`
- 測試(子集):`cabal test --test-options='-m "P-001"'`
- 模組前綴:`Game`
- 原始碼根目錄:`src-<層>`
- IO 模組追加:無
- 效果型別追加:無
- 忽略目錄:`old`
- 號段:無
- 套件與框架:cborg(CBOR 編解碼);不引入其他序列化套件
- 優先:1 = 讀寫的正確性;2 = 使用者看得到的;3 = 呈現;4 = 工具
````

# modules.md

````markdown
# 模組表

## 邊界
- types:`Game.World`(遊戲世界的不可變值,含渲染快取)、`Game.Save.State`(可存檔的投影)
- effect:無
- core:`Game.Save.Core.*`(投影與編解碼)
- shell:`Game.Save.Host`(存檔進入點)、`Game.FS`(檔案系統)

## 模組單元
| 模組 | 層 | 職責 |
|---|---|---|
| `Game.World` | types | 遊戲世界的不可變值與它的實體 |
| `Game.Save` | types、core、shell | 可存檔的投影、編解碼與存檔的進入點 |
| `Game.FS` | shell | 檔案系統:原子寫檔 |

## 對外 I/O
| 名稱 | 方向 | 型別 / 效果 ADT | shell 模組 | 進入哪條 pipeline |
|---|---|---|---|---|
| 存檔檔案 | out | `ByteString` | `Game.FS` | P-001-save-write |
````

# lawful status 裡講到 M-2 的每一行(整份報告要看再跑 lawful status)

### lawful status
願景:玩家在任何時刻存檔,之後讀回來的世界和存檔那一刻一模一樣。
需求 2 條,達成 1 條(測試 1、推得 0)· 領域不變量 0 條,成立 0 條 · 里程碑 4 條,達成 0 條 · io pipeline 1 條,達成 0 條 · pipeline 1 條,達成 0 條 · 模組單元 3 個 · 還沒實作的 stage 4 個 · 還開著的 GAP 0 條
· 測試結果來自 test.log

#### 需求
| 需求 | 優先 | 一句話 | 驗收 | 依賴 | 里程碑總數 | 里程碑達成 | 完成度 |
|---|---|---|---|---|---|---|---|
| R-1 | 1 | 玩家存檔後能讀回同一個世界 | 達成(測試 R-1#ACCEPT green) | - | 4 | 0 | 0% |
- R-1 下一個里程碑:M-1 存檔寫得出檔案(P-001-save-write 進行中)

#### 6. 警訊
| 哪裡 | 什麼事 | 怎麼辦 |
|---|---|---|
| M-2 | 沒有英文名,切片開不了分支 | lawful:require-design 把第一格寫成 M-2-<slug>(kebab-case 英文);切片的分支 build/M-2-<slug> 與決策紀錄以它為鍵 |
exit 0
