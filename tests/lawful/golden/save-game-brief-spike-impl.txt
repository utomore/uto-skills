$ lawful brief spike-impl M-2-save-inspect --tests test.log --no-rules
brief spike-impl M-2-save-inspect @doc:4d9a17b1 rules:<雜湊>
這一行是指紋:被委派的角色在回報的第一項照抄它。以下是開工要的全部,不必再去找規章、文檔或宣告。

# 規章

(--no-rules:規章的節這一場已經給過,不重印)

# 分支與工作樹

(這個目錄不是 git repo 的根,也不是一棵工作樹)

# .lawful/ 現在有什麼

- adr/
  - ADR-001-save-format.md
- Cone.md
- modules.md
- pipelines/
  - P-001-save-write.md
- requirements/
  - R-1-save-roundtrip.md

# 需求檔:.lawful/requirements/R-1-save-roundtrip.md

````markdown
---
id: R-1
priority: 1
updated: 2026-09-05
---
# R-1-save-roundtrip:玩家存檔後能讀回同一個世界

- 驗收:任一 World 存檔再讀回,可存檔的投影一模一樣
  - forall w in World
  - |- decode (saveBytes w) == Right (toSave w)

| 里程碑 | 做到什麼 | 綁定 | 怎麼驗 |
|---|---|---|---|
| M-1-save-write | 存檔寫得出檔案 | P-001-save-write | `cabal run save-demo -- write /tmp/a.sav` |
| M-2-save-inspect | 存檔壞了看得出來 | - | - |
| M-3-save-compact | 存檔檔案壓縮後不超過 1 MB | P-001-save-write | `cabal run save-demo -- size /tmp/a.sav` |
````

# Cone.md(全份)

````markdown
---
language: haskell
updated: 2026-09-05
---
# save-game:把遊戲世界存成檔案再讀回來

## 願景
玩家在任何時刻存檔,之後讀回來的世界和存檔那一刻一模一樣。

替一個小型 2D 遊戲提供存檔與讀檔。玩家按存檔,當前世界寫成一個檔案;讀檔把檔案還原成同一個世界。不處理雲端同步,不處理跨版本升級。

## 全域 Law
不得違反:整個專案任何一條切片、任何一條 pipeline 都要守。三類各住一區,各有一道 lint 自動確認(`lawful lint global` 一次查完);新增、修改、放寬、替換或刪除都要開發者明確批准。

### 領域不變量
- INV-1 [roundtrip] 可存檔的狀態收進什麼實體,就吐出什麼實體
  - forall es in [SavedEntity]
  - |- savedEntities (mkSaveState es) == es

### 架構:四層
- types:`Game.World`(遊戲世界的不可變值,含渲染快取)、`Game.Save.State`(可存檔的投影)
- effect:無
- core:`Game.Save.Core.*`(投影與編解碼)
- shell:`Game.Save.Host`(存檔進入點)、`Game.FS`(檔案系統)

### 契約:對外 I/O
| 名稱 | 方向 | 型別 / 效果 ADT | shell 模組 | 進入哪條 pipeline | 契約 |
|---|---|---|---|---|---|
| 存檔檔案 | out | `ByteString` | `Game.FS` | P-001-save-write | P-001#LAW-5、INV-1 |

## Constraint
硬性限制:寫程式之前就定得下來、每一行程式碼與測試都照做的規定,與工具要讀的那幾行(語言、三道指令、模組前綴、原始碼根目錄、追加清單、忽略目錄、號段、優先)。開發者定,`lawful:kickoff` 寫,之後隨時回 `lawful:kickoff` 補或改;限制的類別可以自己加。
- 語言與版本:Haskell,GHC2021
- 編譯器與執行環境:GHC 9.6、cabal 3.10
- 套件與框架:cborg(CBOR 編解碼);不引入其他序列化套件
- 環境:無
- 命名與寫法:型別與建構子 PascalCase、函數與變數 camelCase;模組一律寫匯出清單;smart constructor 以 `mk` 開頭
- 語言:haskell
- 建置:`cabal build`
- 測試(整套):`cabal test`
- 測試(子集):`cabal test --test-options='-m "P-001"'`
- 模組前綴:`Game`
- 原始碼根目錄:`src-<層>`
- IO 模組追加:無
- 效果型別追加:無
- 忽略目錄:`old`
- 號段:無
- 優先:1 = 讀寫的正確性;2 = 使用者看得到的;3 = 呈現;4 = 工具
````

# modules.md

````markdown
# 模組表

## 模組單元
| 模組 | 層 | 職責 |
|---|---|---|
| `Game.World` | types | 遊戲世界的不可變值與它的實體 |
| `Game.Save` | types、core、shell | 可存檔的投影、編解碼與存檔的進入點 |
| `Game.FS` | shell | 檔案系統:原子寫檔 |
````

# lawful status 裡講到 M-2-save-inspect 的每一行(整份報告要看再跑 lawful status)

### lawful status
願景:玩家在任何時刻存檔,之後讀回來的世界和存檔那一刻一模一樣。
需求 1 條,已驗收 0 條 · 等人工審核 1 條 · 領域不變量 1 條,成立 1 條 · 里程碑 3 條,達成 0 條 · io pipeline 1 條,達成 0 條 · pipeline 1 條,達成 0 條 · 模組單元 3 個 · 還沒實作的 stage 4 個 · 還開著的 GAP 0 條
· 測試結果來自 test.log

#### 需求
| 需求 | 優先 | 一句話 | 審核 | 依賴 | 里程碑總數 | 里程碑達成 | 完成度 |
|---|---|---|---|---|---|---|---|
| R-1 | 1 | 玩家存檔後能讀回同一個世界 | 待審核(證據齊:測試 R-1#ACCEPT green;里程碑 0 / 3 達成;等人工審核) | - | 3 | 0 | 0% |
- R-1 下一個里程碑:M-1-save-write 存檔寫得出檔案(P-001-save-write 進行中)

#### 1. 今天能開幾條線
- P-001-save-write:lawful:build P-001-save-write(R-1 優先 1 · M-1-save-write 存檔寫得出檔案)

#### 3. 等決定
- R-1 待審核:玩家存檔後能讀回同一個世界
  - 證據:測試 R-1#ACCEPT green;里程碑 0 / 3 達成
  - 怎麼驗:`cabal build` → M-1-save-write `cabal run save-demo -- write /tmp/a.sav` → M-2-save-inspect 沒有怎麼驗欄(lawful:require-design 補一道指令) → M-3-save-compact `cabal run save-demo -- size /tmp/a.sav` → 看「任一 World 存檔再讀回,可存檔的投影一模一樣」成不成立
  - 認可:lawful requirement accept R-1 --by <你的 email> --evidence "<憑什麼認的,一句>"

#### 6. 警訊
| 哪裡 | 什麼事 | 怎麼辦 |
|---|---|---|
| R-1 | 等人工審核,M-2-save-inspect 沒有「怎麼驗」欄,審的人沒有手段可以跑 | 切片收尾時 lawful:integrate 會把決策紀錄的 Entry 那道指令寫進怎麼驗欄;現在補:lawful requirement verify <M-n-slug> "<指令>" |

#### 7. 建議路線
1. lawful:build P-001-save-write(R-1 優先 1 · M-1-save-write 存檔寫得出檔案)
2. 請開發者親自審核 R-1(測試 R-1#ACCEPT green;里程碑 0 / 3 達成;怎麼驗見第 3 段),認可了才 lawful requirement accept R-1 --by <email> --evidence "<一句>"
exit 0
